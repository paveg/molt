import {
  "moonbitlang/async",
  "moonbitlang/async/aqueue",
}

options(
  "supported-targets": "native",
  targets: {
    "rate_limiter.mbt": [ "native" ],
    "rate_limiter_wbtest.mbt": [ "native" ],
  },
)
